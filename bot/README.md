# Ответ бота на /start

Приложение открывается кнопкой у поля ввода, и её почти никто не замечает. Описание, которое ставит
деплой, видно только **до** нажатия Start — после него чат пустой. Человек, которому дали имя бота,
заходит и не понимает, что делать.

Эта папка — то, что отвечает на первое сообщение: одна функция, которая присылает короткий текст и
кнопку «Открыть курсы».

## Почему отдельно от остального

Отвечать на сообщение может только процесс, который слушает обновления. Сайт — это файлы на
статическом хостинге, слушать там нечему. Cloudflare Workers дают бесплатный тариф без карты, и
этого хватает с запасом: сто тысяч запросов в сутки против нескольких десятков.

## Токена здесь нет

Telegram выполняет метод, записанный в **тело ответа** на его же запрос. Ответ уезжает по тому
соединению, по которому пришло обновление, поэтому функции не нужен токен бота — и терять здесь
нечего. Это относится ко всему, что воркер делает, включая отзывы: см. «Обратная связь».

Защита в том, что адрес известен только Telegram: `WEBHOOK_PATH` — случайная строка, и запрос по
любому другому пути игнорируется.

## Как поставить, один раз

Нужен аккаунт Cloudflare — бесплатный, карта не требуется.

```bash
cd bot
npx wrangler login
```

Придумайте случайную строку для адреса — например, из 32 символов:

```bash
node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
```

Сохраните её как секрет и разверните:

```bash
npx wrangler secret put WEBHOOK_PATH
npx wrangler deploy
```

Wrangler напечатает адрес вида `https://bankrate-tj-bot.<ваше-имя>.workers.dev`.

Теперь скажите Telegram, куда слать обновления. Подставьте свой токен, адрес и ту же строку:

```bash
curl "https://api.telegram.org/bot<ТОКЕН>/setWebhook?url=https://bankrate-tj-bot.<ваше-имя>.workers.dev/<СТРОКА>"
```

В ответе должно быть `"ok":true`. Проверить, что Telegram доволен:

```bash
curl "https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo"
```

`pending_update_count: 0` и пустой `last_error_message` означают, что всё работает.

## Развёрнуто

Адрес: `https://bankrate-tj-bot.shohruhmahkamov28.workers.dev`

Проверено 19.08.2026: отвечает на русском, таджикском и узбекском, неизвестный язык откатывается на
русский, запрос по любому другому пути и обращение браузером получают `ok` и ничего больше.

## Обратная связь

Отзыв пишут внутри приложения: «О проекте» → «Написать нам» → имя и текст → «Отправить». Ответ —
«Запрос принят». Форма шлёт `POST` на `/feedback` этого же воркера.

Всё, что человек напишет боту в чате, кроме первой команды, считается сказанным нам: бот
благодарит и кладёт текст туда же.

### Почему без токена

Уведомить владельца в момент отзыва — единственное, что нельзя сделать ответом на чужой запрос:
браузер обращается к нам, а не Telegram, и ответить в теле нечему. Это стоило бы токена, а токен
надо где-то держать, менять при утечке и один раз правильно ввести.

Поэтому отзыв ложится в хранилище, а владелец забирает накопленное командой:

```
/otzyvy
```

Бот отвечает списком: кто, когда, откуда — из приложения или из чата, — и сам текст. Последние 12,
новые сверху. Команда работает только в чате владельца: сравнивается `OWNER_CHAT_ID`, и посторонний
получает обычное «спасибо», а не чужие отзывы.

Цена решения — уведомление не приходит само. Надо спросить.

### Что нужно настроить

Хранилище (создаётся один раз, `id` уже прописан в `wrangler.toml`):

```bash
npx wrangler kv namespace create FEEDBACK
```

И номер чата владельца:

```bash
npx wrangler secret put OWNER_CHAT_ID
npx wrangler deploy
```

`OWNER_CHAT_ID` — тот же номер, что лежит в секретах GitHub под именем `TELEGRAM_CHAT_ID`.

Без хранилища форма отвечает ошибкой, а не благодарностью: ложное «спасибо» хуже отказа, потому что
человек перестаёт гадать, дошло ли, и больше не пишет. Бот в чате при этом всё равно благодарит —
там человек видит своё сообщение и без нас.

Записи Cloudflare стирает сам через 120 дней, поэтому команды «удалить» нет и промахнуться ею
нельзя.

## Проверка

Откройте бота с аккаунта, который его ещё не открывал, и нажмите Start. Должен прийти текст и кнопка.

## Если адрес приложения поменяется

`APP_URL` в `worker.js` — единственное место, где он записан. После правки `npx wrangler deploy`.
