node_modules
dist
dist-static
.vite
.DS_Store
Thumbs.db
*.log
.env
backend/prisma/dev.db
backend/prisma/dev.db-journal

# The 4.7MB design mock-up. Kept on disk as the reference to build against, but a bundled React
# runtime does not belong in the history of the app it is a picture of.
design/

# Картинки для постов пересобираются из данных: scripts/make-post.js. Держать в репозитории копии
# вчерашних курсов незачем — они устареют раньше, чем кто-то их откроет.
posts/
