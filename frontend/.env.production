# In production the API and the built frontend are served by the same Express process on one
# origin, so the base URL must be relative — a hardcoded host would break the moment the site
# moves to a real domain.
VITE_API_BASE_URL=/api
